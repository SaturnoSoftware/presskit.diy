# @presskit-diy/utils

Reusable JavaScript helpers extracted from `presskit.diy`.

## Scope

This package currently provides four small helper modules:

- `path-utils`
- `color-console`
- `sfs`
- `zip`

It is intentionally generic and does not depend on Saturno-specific build or deployment infrastructure.
